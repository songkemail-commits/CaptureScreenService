## 修复计划

### 1. 为卸载程序添加管理员权限请求

* 创建 `app.manifest` 文件，配置 `requireAdministrator` 执行级别

* 修改 `Uninstaller.csproj` 引用该清单文件

### 2. 改进卸载程序启动逻辑

* 在窗体构造函数中添加异常处理

* 确保卸载流程能正常启动

### 3. 验证安装程序的资源嵌入

* 确认 `build.bat` 正确复制 `uninstall.exe` 到 `ServiceFiles` 目录

* 确认 `Installer.csproj` 正确嵌入资源

### 文件变更：

1. **新建** `Uninstaller/app.manifest` - 请求管理员权限
2. **修改** `Uninstaller/Uninstaller.csproj` - 引用清单文件
3. **修改** `Uninstaller/Program.cs` - 改进错误处理和启动逻辑

